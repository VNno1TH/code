#include <algorithm>
#include <bits/stdc++.h>
#include <climits>
#include <cmath>
#include <future>
#include <iterator>
#include <random>
using namespace std;

#define ll long long
#define pb push_back
#define all(x) (x).begin(), (x).end()
#define FOR(i, a, b) for (int i = (a); i < (b); ++i)
#define endl '\n'
// n<=m
// ci=ai-b(i+j)
// 0<=j<=m-n
int n, m, q;
vector<int> a, b;

int32_t main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  cin >> n >> m >> q;
  a.resize(n);
  b.resize(m);
  FOR(i, 0, n) cin >> a[i];
  FOR(i, 0, m) cin >> b[i];

  ì
}
